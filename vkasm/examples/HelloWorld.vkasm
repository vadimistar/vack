sec .consts

"Hello World!."

sec .code

call main
call exit

main:
	push 0 ; 0
	dup ; 0 0
loop:
	getconst ; 1 e
	dup ; 1 e e
	push 46 ; 1 e e .
	if_eq exit ; 1 e e .
	
	logb ; 1 e
	pop ; 1
	addu ; 1
	dup ; 1 1

	goto loop
	
	ret

exit:
	halt
